#include<conio.#include<stdio.h>
#include<iostream>
using namespace std;

  class mix
 {  
    public:
     
    int result()
    
    {   int i,j,p,n,a[100],smoke[100][100],test,count;
           
     while(scanf("%d",&n)
     {   
         for(i=0;i<n;i++)
         { 
             scanf("%d",&a[n]);
         } 
		  for(i=0;i<n;i++)
         { 
              for(j=0;j<n;j++)
              {
			       smoke[i][j]=a[i]*a[j];;
			  } 
         } 
		  for(i=0;i<n;i++)
         { 
              for(j=0;j<n;j++)
              {
			       if(min      
			  } 
         } 
		 
		 
		 for()                                                                 
        (a[i]*a[i])%100; 10*10<100,
     }
}
};

int main()
{mix a;
 a.result();
 return 0;
}