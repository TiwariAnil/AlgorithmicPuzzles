#include<stdio.h>
#include<conio.h>
//using namespace std;

main()
{
      int x,num;
      x=scanf("%d",& num); // returns 0 or 1 , if executed or not
      printf("%d", x);
      x=printf("%d",num);  // retcuted urns the no of characters printed  
      printf("%d", x);
      getch();
      
}        
